# 视觉 QA · 验收修复（2026-06-25）

针对用户验收反馈的修复批次。视口固定 `1920×1080`，`--force-device-scale-factor=1` 无缩放截图；数据 fixture 为 `topic=1`（党史）、`event=1`（鸦片战争）。

## 截图

| 文件 | 固定 URL | 验证点 |
|---|---|---|
| `01-read-editor-first.png` | `…/?topic=1&event=1&mode=view` | 右栏阅读态：编辑器优先，仅 标题+日期/分期+正文；空的 标签/附件/关联段不渲染。三栏顶部分隔线对齐，中栏左上显示笔记本名「党史」。 |
| `02-edit-seamless.png` | `…/?topic=1&event=1&mode=edit` | 右栏编辑态：与阅读态零位移（无"时间栏"），日期/分期为文字触发器；顶部新增 `# / 📎 / 🔗` 功能按钮 + 保存（仅编辑态）。 |
| `03-two-column-default.png` | `…/?topic=1` | 默认两栏（右栏收起），中栏占满；无横向滚动条。 |

## 需交互验证的状态（已在浏览器实拍确认，非静态截图）

- **日期/分期 popover**：点击日期文字触发器弹出标准浮层（`--bg-detail` + 1px 边 + `--radius-lg` + `--shadow-pop`），内含无边框输入框。
- **无边框输入**：日期年/月/日、标签、关联搜索、中栏搜索框均 `border:0` + `--bg-surface-2` 柔底 + 13px，聚焦加 inset 强调环。
- **中栏实时预览**：编辑态改日期/分期，中栏对应行实时重排/重组，分期范围副标题同步更新。
- **关联事件**：编辑态默认不铺候选，点 `🔗` 才展开搜索。

## 门禁

`agent:check` ✓ ／ `build` ✓ ／ `test:ui` 23‑23 ✓ ／ `pytest` 9‑9 ✓。
