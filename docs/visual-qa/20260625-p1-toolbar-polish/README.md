# 视觉 QA · P1 三栏轻量打磨（2026-06-25）

整改 P1：中栏搜索框、左栏折叠切换、中栏标题、图标分级、底栏精致化。
固定 URL：`http://127.0.0.1:8798/?topic=1&event=1`；数据 fixture：`topic=1`（党史）、`event=1`（鸦片战争）。

> 截图说明：本批在“用户当前运行的 Chrome（系统 125% 缩放）”实机会话中验证，实拍视口约 `1568×777`，**无法复现既往批次的 `--force-device-scale-factor=1` 无缩放 `1920×1080` 静态 PNG**。布局为单页自适应（无 `transform:scale()` 承载布局），该视口下验证具代表性。以下状态均在浏览器**实拍确认**。

## 实拍确认的状态

| 状态 | 操作/URL | 验证点 |
|---|---|---|
| 搜索收起 | 默认 | 中栏工具条搜索为**完整放大镜**图标（修复前曾被收起态输入框 padding 挤出、`overflow:hidden` 裁成「C」形）。 |
| 搜索展开 | 点搜索图标 | **Notion 式一体化药丸**：图标 + 输入同一圆角容器、单一柔底 + 聚焦环（无 1px 边框）；图标固定在药丸右端，整条**向左展开**；其它工具图标与右栏宽度**不位移**；输入「鸦」实时过滤为 2 条。 |
| 折叠切换 | 点 pane-head 折叠键 | 「视图/笔记本」分组与笔记本节点**全部折叠**，按钮图标 `fold→unfold`、title「全部折叠↔全部展开」同步；再点**全部展开**复原。 |
| 标题一行 | 默认 | 中栏左上为单行 `党史 · 共 56 条`（已删「时间线」前缀）；窄栏先省略标题不挤按钮。 |
| 图标分级 | 默认 | 三栏顶部工具条图标 `--icon-bar:16px` 明显**小于**最左 ribbon `--icon-rail:18px`。 |
| 底栏 | 默认 | 左下「编年/帮助/设置」图标尺寸统一、知识库图标弱化配色 + hover；功能深化（切换器/设置 modal）留待 P3/P5。 |

## 门禁

`agent:check` ✓ ／ `build` ✓ ／ `test:ui` 23‑23 ✓ ／ `pytest` 9‑9 ✓。

## 已知偏差

- 静态 `1920×1080` PNG 未归档（本机缩放会话限制，见上）；如需，可在 `--force-device-scale-factor=1` 的受控 Chrome/Playwright 会话补拍。
