# P3 视觉/交互 QA · 统一属性系统

日期：2026-06-26 ｜ 阶段：P3（类型/标签/自定义列统一为「属性」+ 全量迁移）

## 验证环境
- 后端：`python backend/server.py`（启动期对真实 `data/timeline.db` 执行属性迁移；迁移前已备份至 session scratchpad `timeline.db.p3bak`）。
- 前端：`npm run build` 产物，由后端托管于 `http://127.0.0.1:8000`。
- 浏览器：Chromium（Edge）经 Claude-in-Chrome，视口 1912×948。

## 像素截图限制（同 P1）
本机 CDP `Page.captureScreenshot` 持续 30s 超时，无法导出像素截图；因此本轮以**可访问性树（read_page）+ 控制台错误 + 路由状态 + API**做结构化与交互验证。`1920×1080` 固定 URL 像素归档在此环境仍不可得。

## 已验证（结构 + 交互）
- 左栏 ribbon「标签」已更名「属性」（`sliders` 图标）；点击后渲染按属性分组的选项行（党史：3 个标签选项）。
- 左下/中上工具条完整；右栏编辑态去掉了旧「添加标签」按钮。
- **Req5 情境删除**：树面板下出现「删除当前笔记本」按钮（位于「新建笔记」左侧），非树面板（属性/统计）下自动隐藏。
- **属性=值筛选闭环**：点击「属性」Tab 中 `标签 · 战争` → 路由变为 `?topic=1&pk=tags&pv=war`（旧 `tag` 参数已废弃，空列表 bug 经稳定计数修复）。
- **右栏统一「属性」区**：编辑态渲染 类型(单选)+标签(多选) 两个 OptionPicker 控件；标签控件显示已选「战争」chip + 移除按钮。
- **OptionPicker 闭环**：点击控件弹出层含「搜索或新建…」输入 + 既有选项（战争/政治/文化）+（无匹配时）新建项。
- 全程 **0 控制台错误/异常**。

## API / 迁移验证（见提交说明）
- 迁移在 `probe.db` 与真实库均通过：`tags_json` 列已 DROP、208 事件/256 条目完整、外键完整性空、无 `_rebuild_` 残留、幂等。
- 写入闭环（新增选项→事件引用→未知 option id 丢弃→重载持久化）经 API 实测通过；测试痕迹已还原。

## 已知差异 / 待跟进
- 中栏选项单元格当前为**只读 chips**（编辑在右栏）；行内 OptionPicker 编辑列为 P3 后续打磨项。
- 像素级视觉归档待具备无缩放截图环境后补齐。
